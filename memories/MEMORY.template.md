<!--
  MEMORY.md — durable notes about the environment AutoSys Architect operates in.

  Capped at ~2200 chars by Hermes upstream. Edit via the `memory` tool
  ('add' / 'replace' / 'remove'); never via shell or write tools — Hermes
  scans every change for prompt-injection patterns and invisible Unicode.

  Good entries:
    - Project facts that don't change often ("user's main repo is X")
    - User preferences explicitly stated and confirmed
    - Architecture decisions and their reasoning
    - Long-lived TODOs ("we agreed to revisit X after Y")

  Bad entries (do NOT store):
    - Anything resembling a secret, token, password, or PII
    - Specific tool outputs (re-query when needed)
    - Temporary errors or one-off requests
    - Speculation or unverified claims
-->
